<Window x:Class="UI.EditCategoriesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="300"
        Height="400"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

    <Border Background="GhostWhite" 
            CornerRadius="20" 
            BorderBrush="#CCCCCC"
            BorderThickness="1">

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="1" 
                       Text="Редактирование"
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="Black"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="CategoriesPanel" Margin="0,10,0,10"/>
            </ScrollViewer>

            <StackPanel Grid.Row="3" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Center">

                <Button Content="Удалить выбранные"
                        Width="120"
                        Height="25"
                        Background="GhostWhite"
                        Foreground="Black"
                        BorderThickness="1"
                        Click="DeleteSelectedCategories_Click"/>

                <Button Content="Новая"
                        Width="60"
                        Height="25"
                        Background="GhostWhite"
                        Foreground="Black"
                        BorderThickness="1"
                        Margin="10,0,0,0"
                        Click="NewCategoryButton_Click"/>

            </StackPanel>


            <!-- круглая кнопка выхода -->
            <Button Width="20" Height="20" 
                    Click="CloseButton_Click" 
                    Background="LightGray" 
                    BorderBrush="Transparent" 
                    HorizontalAlignment="Right">

                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse Fill="{TemplateBinding Background}"/>
                            <Viewbox>
                                <Canvas Width="32" Height="32">
                                    <Path Stroke="White" StrokeThickness="3" Data="M 8,8 L 24,24 M 24,8 L 8,24"/>
                                </Canvas>
                            </Viewbox>
                        </Grid>
                    </ControlTemplate>
                </Button.Template>
            </Button>

        </Grid>
    </Border>
</Window>
