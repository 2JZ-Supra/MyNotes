<Window x:Class="UI.NoteEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Редактирование заметки" 
        Height="500" 
        Width="600"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                Background="#f8f8f8" 
                Padding="10"
                BorderBrush="#cccccc"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Заголовок:" 
                           Style="{StaticResource LabelStyle}"
                           Grid.Column="0"/>

                <TextBox x:Name="TitleTextBox" 
                         Grid.Column="1"
                         Margin="10,0,0,0"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" 
                Background="#f8f8f8" 
                Padding="10"
                BorderBrush="#cccccc"
                BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <CheckBox x:Name="FavoriteCheckBox" 
                          Content="Избранное"
                          VerticalAlignment="Center"
                          Margin="0,0,20,0"/>

                <Button x:Name="SelectCategoriesButton" 
                        Content="Выбрать категории"
                        Style="{StaticResource ButtonStyle}"
                        Click="SelectCategoriesButton_Click"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" 
                Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Текст заметки:" 
                           Style="{StaticResource LabelStyle}"
                           Grid.Row="0"/>

                <TextBox x:Name="ContentTextBox" 
                         Grid.Row="1"
                         Margin="0,5,0,0"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"/>
            </Grid>
        </Border>

        <Border Grid.Row="3" 
                Background="#f8f8f8" 
                Padding="10"
                BorderBrush="#cccccc"
                BorderThickness="0,1,0,1"
                MaxHeight="100">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="CategoriesPreviewText" 
                           TextWrapping="Wrap">
                    <Run Text="Выбранные категории: "/>
                    <Run x:Name="CategoriesListRun" Text="нет"/>
                </TextBlock>
            </ScrollViewer>
        </Border>

        <Border Grid.Row="4" 
                Background="#f8f8f8" 
                Padding="10"
                BorderBrush="#cccccc"
                BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelButton" 
                        Content="Отмена"
                        Style="{StaticResource ButtonStyle}"
                        Click="CancelButton_Click"/>

                <Button x:Name="SaveButton" 
                        Content="Сохранить"
                        Style="{StaticResource ButtonStyle}"
                        Background="#4CAF50"
                        Foreground="White"
                        Click="SaveButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>