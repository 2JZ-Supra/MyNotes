<Window x:Class="UI.NewCategory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Новая категория" Height="180" Width="350">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Название категории:" Margin="0 0 0 5"/>
        <TextBox x:Name="CategoryNameTextBox" Grid.Row="1" Height="25"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0 10 0 0">
            <Button Content="OK" Width="70" Margin="0 0 10 0"
                    Click="OkButton_Click"/>
            <Button Content="Отмена" Width="70"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
